<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dhwon.payflow_api.api.paymentFail.mapper.PaymentFailMapper">

    <!-- =====================================================
         실패 목록 조회
    ====================================================== -->

    <select id="selectPaymentFailList"
            resultType="com.dhwon.payflow_api.api.paymentFail.dto.PaymentFailSelectResponseDto">

        SELECT
        P.MERCHANT_ID,
        P.BASE_DATE,
        P.TX_SEQ,
        P.FAIL_CODE,
        P.FAIL_MESSAGE,
        P.CREATED_AT
        FROM PAYMENT_FAIL P
        WHERE 1=1

        <if test="dto.merchantId != null and dto.merchantId != ''">
            AND P.MERCHANT_ID = #{dto.merchantId}
        </if>

        <if test="dto.baseDateFrom != null and dto.baseDateFrom != ''">
            AND P.BASE_DATE <![CDATA[>=]]> #{dto.baseDateFrom}
        </if>

        <if test="dto.baseDateTo != null and dto.baseDateTo != ''">
            AND P.BASE_DATE <![CDATA[<=]]> #{dto.baseDateTo}
        </if>

        ORDER BY
        P.BASE_DATE DESC,
        P.MERCHANT_ID,
        P.TX_SEQ

        OFFSET #{dto.offset} ROWS
        FETCH NEXT #{dto.size} ROWS ONLY

    </select>

    <!-- =====================================================
         실패 목록 COUNT
    ====================================================== -->

    <select id="selectPaymentFailListCount"
            resultType="int">

        SELECT COUNT(*)
        FROM PAYMENT_FAIL P
        WHERE 1=1

        <if test="dto.merchantId != null and dto.merchantId != ''">
            AND P.MERCHANT_ID = #{dto.merchantId}
        </if>

        <if test="dto.baseDateFrom != null and dto.baseDateFrom != ''">
            AND P.BASE_DATE <![CDATA[>=]]> #{dto.baseDateFrom}
        </if>

        <if test="dto.baseDateTo != null and dto.baseDateTo != ''">
            AND P.BASE_DATE <![CDATA[<=]]> #{dto.baseDateTo}
        </if>

    </select>

</mapper>